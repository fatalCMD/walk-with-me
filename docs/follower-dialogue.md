# Follower dialogue

Party Add/Remove and follower dialogue share saved manual enrollment.
Automatic enrollment adds nearby recruited followers by default. Remove prevents re-addition.
Existing INIs keep their setting. Explicit external registrations still work.

Removal releases this mod without dismissing the follower and adds a saved exclusion.
Automatically or integration-managed followers do not get the manual dialogue commands.
External registrations retain their own lifecycle. Detection proves recruitment,
not compatibility with every custom rest system. This change did not diagnose the
reported M'rissi crash. Test add/remove, wait/dismissal and save/load.
