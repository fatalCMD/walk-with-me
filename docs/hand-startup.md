# Hand startup: revision 10

A stationary approach could exist in native code while the quest bridge returned
no follower for its slot. The log showed alias=false and an NFF package.
Revision 10 exposes that approach to the bridge while the player is idle and allows
an aligned stationary pair to connect without first walking. Distance, facing,
visibility and reach checks remain. The video could not be matched to an exact log time.
