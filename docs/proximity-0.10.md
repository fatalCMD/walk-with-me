# Role matching 0.10

Match followers to grounded formation targets using total squared distance.
Each role gets one follower. This is a geometric estimate; Skyrim still finds the
route. Recheck roughly every half second during movement. A switch penalty and
one-second settling interval reduce unnecessary changes. Offsets blend, while
package, marker, alias and speed-hook slots remain stable.
